% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{.dtiGraphQL}
\alias{.dtiGraphQL}
\title{Perform a GraphQL POST with retry + polite throttling}
\usage{
.dtiGraphQL(
  url,
  query,
  variables = list(),
  timeout = 60L,
  maxTries = 3L,
  hardStop = FALSE
)
}
\arguments{
\item{url}{character(1) GraphQL endpoint URL.}

\item{query}{character(1) GraphQL query/mutation document.}

\item{variables}{named list of GraphQL variables.}

\item{timeout}{numeric(1) per-request timeout in seconds.}

\item{maxTries}{integer(1) total attempts including the first.}

\item{hardStop}{logical(1) if TRUE, rethrow the error instead of NULL.}
}
\value{
parsed \code{data} element of the GraphQL response, or NULL.
}
\description{
Shared by any GraphQL-backed source (DGIdb, and later Open Targets).
Mirrors \code{\link{.dtiApiGET}}'s retry/throttle/timeout behavior but
posts a \code{\{query, variables\}} JSON body instead of a query-string
GET. On any transport failure or a GraphQL \code{errors} payload it
warns and returns \code{NULL} (or the partial \code{data} field, for
errors) so callers can degrade gracefully, unless \code{hardStop = TRUE}.
}
\keyword{internal}
