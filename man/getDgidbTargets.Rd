% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{getDgidbTargets}
\alias{getDgidbTargets}
\title{Retrieve DGIdb target interactions for one or more drugs}
\usage{
getDgidbTargets(drugs, pageSize = 500L, maxRows = 5000L, verbose = FALSE)
}
\arguments{
\item{drugs}{character vector of drug names (case-insensitive;
unmatched names simply contribute no rows, no error).}

\item{pageSize}{integer(1) rows per GraphQL page (default 500).}

\item{maxRows}{integer(1) cap on total returned rows (default 5000).}

\item{verbose}{logical(1); if TRUE, message progress per chunk/page.}
}
\value{
A \code{data.frame}; see \code{\link{getDgidbDrugs}} for columns.
}
\description{
Queries the DGIdb \code{interactions(drugNames: ...)} GraphQL field
and returns a tidy \code{data.frame} of drug-gene interaction rows.
This is the drug -> target direction; see \code{\link{getDgidbDrugs}}
for the reverse. Same columns/shape as \code{\link{getDgidbDrugs}}
since \code{Interaction} nodes always carry both endpoints.
}
\examples{
\donttest{
  df <- getDgidbTargets(c("imatinib", "aspirin"))
  table(df$drug_name, useNA = "no")
}
}
\seealso{
\code{\link{getDgidbDrugs}}, \code{\link{getDgidbDrugTarget}}
}
