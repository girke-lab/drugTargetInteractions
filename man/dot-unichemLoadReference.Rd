% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unichemAccess.R
\name{.unichemLoadReference}
\alias{.unichemLoadReference}
\title{Chunked-load UniChem's reference table dump into a staging SQLite table}
\usage{
.unichemLoadReference(refPath, con, chunkSize = 500000L, verbose = FALSE)
}
\arguments{
\item{refPath}{character(1) path to the downloaded \code{reference.tsv.gz}.}

\item{con}{an open \code{RSQLite} connection.}

\item{chunkSize}{integer(1) rows read per batch (default 500,000;
live-tested 2026-07-17 at ~4s/2.3M-row-slice, i.e. ~5 minutes
extrapolated for the full file).}

\item{verbose}{logical(1); if TRUE, message progress per chunk.}
}
\value{
integer(1) total rows loaded (invisibly).
}
\description{
Streams \code{reference.tsv.gz} through a gzip connection in batches
of \code{chunkSize} rows (never materialising the full ~166M-row file
as an in-memory data.frame) and appends each batch to a
\code{ref_raw} table. \code{SRC_COMPOUND_ID} is read as character
throughout - several sources (e.g. SwissLipids' \code{"SLM:..."} IDs)
are not purely numeric.
}
\keyword{internal}
