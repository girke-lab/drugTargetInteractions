% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{.dtiBatchGET}
\alias{.dtiBatchGET}
\title{Batched + paginated GET against a ChEMBL-style list resource}
\usage{
.dtiBatchGET(
  url,
  filterField,
  ids,
  resultsField,
  chunkSize = 200L,
  extraQuery = list(),
  verbose = FALSE
)
}
\arguments{
\item{url}{character(1) resource URL, e.g. `.../mechanism.json`.}

\item{filterField}{character(1) filter query param, e.g.
`"target_chembl_id__in"`.}

\item{ids}{character vector of IDs to filter on (deduplicated
internally; order does not matter, this only fetches records).}

\item{resultsField}{character(1) name of the list field in the JSON
response holding the records, e.g. `"mechanisms"`.}

\item{chunkSize}{integer(1) max IDs per `__in` filter / request.}

\item{extraQuery}{named list of additional fixed query parameters.}

\item{verbose}{logical(1); if TRUE, message progress per batch.}
}
\value{
a flat list of record lists (unparsed JSON records), pooled
  across all chunks/pages.
}
\description{
Many ChEMBL REST list resources (`target`, `mechanism`,
`drug_indication`, `molecule`, ...) accept a `<field>__in=id1,id2,...`
filter. A single request's URL is capped by the server at a request-
line length of ~4KB, which in practice limits a single `__in` filter
to roughly 250-400 IDs depending on ID length (`httr2` percent-encodes
the separating commas, which eats into that budget) — so for larger ID
sets this chunks `ids` into batches of `chunkSize`, and paginates each
batch via `offset`/`page_meta$total_count` until it is exhausted.
}
\keyword{internal}
