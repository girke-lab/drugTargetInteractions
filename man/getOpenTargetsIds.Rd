% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{getOpenTargetsIds}
\alias{getOpenTargetsIds}
\title{Resolve many gene symbols to Ensembl IDs in batched GraphQL requests}
\usage{
getOpenTargetsIds(symbols, chunkSize = 100L, pause = 0.1, verbose = FALSE)
}
\arguments{
\item{symbols}{character vector of gene symbols (Ensembl gene IDs pass
through unresolved - checked via a plain \code{"^ENSG[0-9]+$"} regex).}

\item{chunkSize}{integer(1) symbols per HTTP request (default 100).}

\item{pause}{numeric(1) seconds to sleep between requests (politeness).}

\item{verbose}{logical(1) progress messages.}
}
\value{
A named character vector (names = input symbols) of Ensembl
  gene IDs; unresolved symbols are \code{NA}. Order matches the input.
}
\description{
Uses GraphQL field-aliasing to resolve up to \code{chunkSize} symbols
per HTTP request, so resolving many genes takes a handful of requests
rather than one per gene.
}
\examples{
\donttest{
  getOpenTargetsIds(c("FGFR1", "KLB", "NOT_A_GENE"))
}
}
\seealso{
\code{\link{getOpenTargetsDrugs}}
}
