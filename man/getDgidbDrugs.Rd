% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{getDgidbDrugs}
\alias{getDgidbDrugs}
\title{Retrieve DGIdb drug interactions for one or more genes}
\usage{
getDgidbDrugs(genes, pageSize = 500L, maxRows = 5000L, verbose = FALSE)
}
\arguments{
\item{genes}{character vector of HGNC gene symbols (case-insensitive;
unmatched symbols simply contribute no rows, no error).}

\item{pageSize}{integer(1) rows per GraphQL page (default 500).}

\item{maxRows}{integer(1) cap on total returned rows (default 5000).}

\item{verbose}{logical(1); if TRUE, message progress per chunk/page.}
}
\value{
A \code{data.frame} with columns \code{gene_name},
  \code{drug_name}, \code{drug_concept_id} (e.g.
  \code{"chembl:CHEMBL1201585"}), \code{drug_approved},
  \code{interaction_types}, \code{directionality},
  \code{interaction_score}, \code{evidence_score}, \code{sources}
  (upstream provenance), \code{db}. Empty if none / offline.
}
\description{
Queries the DGIdb \code{interactions(geneNames: ...)} GraphQL field
and returns a tidy \code{data.frame} of gene-drug interaction rows.
This is the target -> drug direction; see \code{\link{getDgidbTargets}}
for the reverse.
}
\examples{
\donttest{
  df <- getDgidbDrugs(c("FGFR1", "KLB"))
  table(df$gene_name)
}
}
\seealso{
\code{\link{getDgidbTargets}}, \code{\link{getDgidbDrugTarget}}
}
