% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiAccess.R
\name{.dtiDgidbFetch}
\alias{.dtiDgidbFetch}
\title{Paginated, chunked interactions() query, shared by both direction wrappers}
\usage{
.dtiDgidbFetch(
  names,
  by = c("gene", "drug"),
  pageSize = 500L,
  maxRows = 5000L,
  chunkSize = 300L,
  verbose = FALSE
)
}
\arguments{
\item{names}{character vector of gene symbols or drug names.}

\item{by}{character(1) \code{"gene"} or \code{"drug"} - which
\code{interactions()} filter argument \code{names} populates.}

\item{pageSize}{integer(1) rows per GraphQL page (cursor-paginated).}

\item{maxRows}{integer(1) cap on total returned rows.}

\item{chunkSize}{integer(1) max names sent per request.}

\item{verbose}{logical(1); if TRUE, message progress per chunk/page.}
}
\value{
list of raw \code{Interaction} GraphQL nodes (unparsed).
}
\description{
\code{names} is sent as a single GraphQL array variable per request (no
URL-length constraint the way ChEMBL's REST \code{__in} filters have),
but is still chunked defensively at \code{chunkSize} - consistent with
the batching convention used for the other REST sources - since DGIdb
publishes no documented cap on array size. Each chunk is paginated via
its own cursor until exhausted.
}
\keyword{internal}
