import(RSQLite,methods)

#these two conflict on 'select', so make sure all calls are prefixed
import(UniProt.ws)
import(dplyr)

import(biomaRt)
import(ensembldb)
import(stats, utils)
import(BiocFileCache)


exportPattern("^[[:alpha:]]+")
