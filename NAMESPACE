import(RSQLite,methods)
import(UniProt.ws)
import(biomaRt)
import(ensembldb)
import(stats, utils)
import(BiocFileCache)
# don't import dplyr here as select function will enterfere with ensembldb and cause errors
# just prefix dplyr functions with dplyr:


exportPattern("^[[:alpha:]]+")
